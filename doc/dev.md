# Develop Document

## TODO
- [ ] token level supervision
    - [ ] EOT token `<|stop|>`
    - continue_with chosen VS input
    - two type of rejected token's loss
        1. $log(p_{rej})$
        2. $-log(1-p_{rej})$


## DONE

